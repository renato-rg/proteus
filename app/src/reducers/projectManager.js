import { } from '../actions'

const initialState = {
    projectOpened: true,
    projectPath: '',
    previousProjects: []
}

function projectManager(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}

export default projectManager
